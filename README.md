Add the following to `.env.local` and `.env.production`:

```
DB2_DSN=
```

After npm install, add a DB2Connect v12 license file to this folder:

`/node_modules/ibm_db/installer/clidriver/license`

https://www.npmjs.com/package/ibm_db